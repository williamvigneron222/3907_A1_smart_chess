#include "Arduino.h"
#include "Preferences.h"
#include "esp32-rmt-ir.h"


#include "adc.h"

void setup()
{
    // Serial.begin(115200); /// IFDEF DEBUG  ?
    // pinMode(GPIO, INPUT);
}


void loop()
{
    // int bit = digitalRead(GPIO);
    // Serial.println();

}